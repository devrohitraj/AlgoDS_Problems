import sys
a={}
n=int(input())
for ii in range(n):
    name,phone=raw_input().split()
    a[name]=phone
lines = sys.stdin.readlines()
for name in lines:
    name=name.strip()
    if name in a.keys():
        print "%s=%s"%(name,a[name])
    else:
        print 'Not found'
